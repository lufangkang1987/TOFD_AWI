## 2026-06-01

### 项目未使用文件审计
- 分析了 .sln 中 10 个项目的引用关系和使用情况
- **可移除**: Video(0个using)、ClimbVideo(独立exe无引用)、MVSDK(仅被ClimbVideo用)、NewFramework/(废弃分支)
- **需加入.sln管理**: ClassLib_DataMang、Cmm_PcPower、HD850_64、ReportDLL (暗依赖)
- **根目录残留**: From/、Class/、Program.cs/Program_New.cs 不在 csproj 中
- NewInspect.Services 未被 Tofd_AWI.csproj 引用，处于"预备项目"状态

### Bug修复
- `HardwareAdapters.cs:158` — `MoveRight()` 缺少 `public` 修饰符，导致接口实现报错
- 修复: `void MoveRight()` → `public void MoveRight()`
- 编译通过，0 错误

### Bug修复: ClassLibrary_Interface 遗漏文件补齐
- `.csproj` 旧式项目不自动包含 .cs 文件，导致 3 个文件在目录但未编译:
  1. `ClassInterFace.cs` — 第1轮补上
  2. `ClsMsgInterFace.cs` — 第2轮补上 (MsgInterFace 类)
  3. `Models/InfrastructureModels.cs` — 第3轮补上 (ClDog/ClPrintItem/ClPrintRecord/XjGetData 等类)
- 每个修复后编译均通过，0 错误
- 至此 ClassLibrary_Interface 项目所有 .cs 文件全部在编译列表中

### Bug修复: NewInspect.Services 项目整体补齐
- `State/` 目录为空，MotionState/TofdState/CScanState 等 6 个状态类从未创建
  - 从 NewFramework/State/RuntimeState.cs 迁移，调整 namespace: `Tofd_AWI.NewFramework.State` → `NewInspect.Services.State`
  - 类型名修正: `Cl_Print_Item` → `ClPrintItem` (ClassLibrary_Interface.Models)
- `.csproj` 遗漏 3 个文件:
  - `State/RuntimeState.cs` — 新增
  - `Services/DataService.cs` — 存在但未编译，补充 `using ClassLib_TestData;`
  - `Utilities/SysUtility.cs` — 存在但未编译
- 缺少 DLL 引用: 补充 `Frame_Work.dll` 和 `ClassLib_TestData.dll` 的 HintPath
- 编译通过: 0 错误 2 警告
