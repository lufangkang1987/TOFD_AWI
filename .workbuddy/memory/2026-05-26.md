# 2026-05-26 工作日志

## 项目框架分析
- 分析了 Tofd_AWI 项目的完整框架结构
- 识别出核心问题：SysInfo.cs 290KB 上帝对象、bin/Debug DLL 路径引用、窗体直调硬件

## 新框架设计与落地
- 设计了分层架构：UI → Services → Adapters(接口) → Hardware DLL
- 在 VS 解决方案中创建了 NewInspect.Services 类库项目
- 项目 GUID: {EE7324D7-B900-4C28-866B-5BAD3A67D598}

### 编译错误修复 — SysInfo 迁移遗留问题 (16:27)
- **错误1**: `ClassInterFace` 在 `ClassLib_TestData` 和 `ClassLibrary_Interface` 之间不明确引用
  - 根因: `GridHelper.cs` 同时 `using ClassLib_TestData;` 和 `using ClassLibrary_Interface;`，两个命名空间都有 `ClassInterFace`
  - 修复: `GridHelper.cs:24` — 改为 `new ClassLibrary_Interface.ClassInterFace()` 完全限定
  
- **错误2**: 未能找到 `Cl_Print_Item` 和 `Cl_Print_Record`
  - 根因: InfrastructureModels.cs 改名为 `ClPrintItem`/`ClPrintRecord`，属性名也变了 (`strItemName`→`ItemName`, `blCheck`→`IsChecked`, `strRecordName`→`RecordName`)，但 GridHelper.cs 仍用旧名
  - 修复: GridHelper.cs 两处 GrdAddData 重载改用 `ClassLibrary_Interface.Models.ClPrintItem` 和 `ClassLibrary_Interface.Models.ClPrintRecord`，同步修正属性名

- **错误3**: `NewInspect.Services.dll` 元数据文件缺失
  - 根因: `ClassLib_DataMang.csproj` 目标框架 v4.5.2，环境中无对应 SDK
  - 修复: 升级 `ClassLib_DataMang.csproj` TargetFrameworkVersion 从 v4.5.2 → v4.8

- **错误4-7**: SysUtility.cs — struct null 比较 (CS0019) + switch 递归模式 (CS8370)
  - `SEmatChanParam` 是结构体，不能用 `== null` / `!= null` 比较 → 直接移除 null 检查
  - `switch expression` 需 C# 8.0+ → 改为传统 switch 语句

- **编译结果**: NewInspect.Services.dll ✅ 0 error, 2 warnings (预存)

### 新增视频 + 波形显示组件 (16:49)
- 用户需求: Program_New.cs 中新增两个视频显示组件 + 一个波形显示组件
- **VideoDisplayService**: 从 NewFramework/Services/ 迁移到 NewInspect.Services/Services/
  - 两路视频: InspectVideoBox (检测摄像头) + OperatorVideoBox (操作员摄像头)
  - GDI+ 帧渲染、缩放居中、叠加文字(距离/时间)、录像指示器(REC红点)
  - 画面切换 (ToggleInspectVideo/ToggleOperatorVideo)、大/小画面交换 (SwapVideoFeeds)
  - 抓拍功能 (CaptureInspectFrame/CaptureOperatorFrame)
- **WaveformDisplayService**: 从 NewFramework/Services/ 迁移到 NewInspect.Services/Services/
  - 四通道波形: AScanBox/A、BScanBox/B、CScanBox/C、DScanBox/D
  - A 扫描: 时域波形 + 闸门标记 + 网格刻度渲染
  - B 扫描: 截面图缩放居中
  - C 扫描: 二维深度映射, 256色热力图配色表 (蓝→青→绿→黄→红)
  - D 扫描: 端视图波形
  - 可配置: 颜色、线宽、网格分割数
- **Program_New.cs**: Step 6b 新增 videoDisplayService + waveformDisplayService 注册, 注入 Frm_Main_C_New
- **Frm_Main_C_New 更新**:
  - 构造函数新增 2 个参数 (VideoDisplayService, WaveformDisplayService)
  - InitializeControls: 6 个 PictureBox 布局 (视频右上/右下 330x250, 波形 450x300 + 450x180)
  - ShowCameraFrame: 改用 VideoDisplayService.UpdateInspectFrame()
  - UpdateAScan: 改用 WaveformDisplayService.UpdateAScan()
  - OnFormClosing: 释放两个显示组件
- **csproj**: 新增 VideoDisplayService.cs 和 WaveformDisplayService.cs 编译项
- **编译结果**: NewInspect.Services.dll ✅ 0 error, 2 warnings (预存)
- 目标框架: .NET Framework 4.8

### 新增文件清单
| 文件 | 位置 | 职责 |
|------|------|------|
| IHardwareInterfaces.cs | ClassLibrary_Interface | 7个硬件接口定义 |
| SystemConfig.cs | ClassLibrary_Interface | INI 配置集中加载 |
| RuntimeState.cs | NewInspect.Services/State | 6个状态类拆分 SysInfo |
| HardwareAdapters.cs | NewInspect.Services/Adapters | 3个硬件适配器 |
| TofdService.cs | NewInspect.Services/Services | TOFD 检测服务 |
| MotionService.cs | NewInspect.Services/Services | 运动控制服务 |
| CameraService.cs | NewInspect.Services/Services | 多路相机服务 |
| DataService.cs | NewInspect.Services/Services | 数据持久化服务 |
| Program_New.cs | Tofd_AWI/ | DI 入口点 |
| Frm_Main_C_New.cs | Tofd_AWI/From/ | 注入式主窗体 |

### 项目引用关系
NewInspect.Services → ClassLibrary_Interface, Frame_Work, Clb_MT_Comm, ClassLib_TestData, ClassLib_DataMang
Tofd_AWI → NewInspect.Services (新增)

### 编译问题修复 (13:02)
- **问题**：编译报错 "未能找到类型或命名空间名 IMotionController"
- **根因**：Tofd_AWI.csproj 中 ClassLibrary_Interface 是旧的 DLL 路径引用 (`HintPath: bin\Debug\ClassLibrary_Interface.dll`)，旧 DLL 不含新增的接口
- **修复**：将 DLL 引用改为项目引用 `<ProjectReference Include="ClassLibrary_Interface\ClassLibrary_Interface.csproj">`
- 该修复同时解决了 Program_New.cs 和 Frm_Main_C_New.cs 中 `using ClassLibrary_Interface` 的类型解析问题

### 编译问题修复 #2 (13:10)
- **问题**：Tofd_AWI.sln 中不包含 ClassLibrary_Interface 项目
- **根因**：原来的 ClassLibrary_Interface 只是 bin\Debug\ 下的 DLL 文件引用，从未加入 .sln。改为 ProjectReference 后 VS 找不到它
- **修复**：
  1. 将 ClassLibrary_Interface.csproj (GUID: {6C68D876-97A7-4D09-9F1F-5D639E066B0D}) 加入 .sln
  2. 添加为 Tofd_AWI 的 ProjectDependency
  3. 添加为 NewInspect.Services 的 ProjectDependency
  4. 添加全部 8 个平台的 Build 配置条目

### 编译问题修复 #3 (13:15)
- **问题**：命名空间"ClassLibrary_Interface"中不存在类型或命名空间名"ClsMsgInterFace"
- **根因#1**：类名不匹配 — ClsMsgInterFace.cs 中定义的类是 `MsgInterFace`，但 HardwareAdapters.cs 引用的是 `ClsMsgInterFace`
- **根因#2**：Tofd_AWI.csproj 仍在用旧的 HintPath DLL 引用，未改为 ProjectReference（上次修复未真正写入）
- **修复**：
  1. HardwareAdapters.cs:34 — `ClsMsgInterFace` → `MsgInterFace`
  2. Tofd_AWI.csproj — DLL HintPath 引用改为 `<ProjectReference Include="..\ClassLibrary_Interface\ClassLibrary_Interface.csproj">`

### 编译问题修复 #8 (13:32)
- **问题**：未能分析方法 InitializeComponent — "未将对象引用设置到对象的实例"
- **根因**：Frm_Main_C_New 只有带 10 个参数的 DI 构造器，WinForms 设计器必须用无参构造器实例化窗体来分析 InitializeComponent，找不到就报 NullReferenceException
- **修复**：添加无参构造器 `public Frm_Main_C_New() { InitializeComponent(); }`，仅供设计器使用；运行时仍走 DI 构造器

### 编译问题修复 #9 — Designer.cs 未注册 (13:42)
- **问题**：Frm_Main_C_New.cs 中 InitializeComponent、_statusTimer、Lb_LinkState 等全部报"当前上下文中不存在"
- **根因**：Frm_Main_C_New.Designer.cs 虽然在磁盘创建了，但 csproj 未注册 `<Compile>` 条目，MSBuild 不编译它，partial class 另一半缺失
- **修复**：在 csproj 中添加 `<Compile Include="From/Frm_Main_C_New.Designer.cs"><DependentUpon>Frm_Main_C_New.cs</DependentUpon></Compile>`

### Designer.cs 重构为设计器兼容格式 (13:45)
- **需求**：改为 VS 设计器原生格式，支持可视化编辑
- **改动**：
  1. Designer.cs — 从对象初始化器简写改为逐属性赋值 + 完整类型名（System.Windows.Forms.XXX）
  2. 添加 AutoScaleDimensions / AutoScaleMode / TabIndex / UseVisualStyleBackColor 等设计器标准属性
  3. PictureBox 添加 ISupportInitialize.BeginInit/EndInit 配对
  4. 事件绑定从 `+= handler` 改为 `+= new System.EventHandler(this.handler)`
  5. _statusTimer 声明保留在 Designer.cs，实例化仍在 InitializeControls，新增 `components?.Add()` 正确注册到组件容器
  6. 移除未使用的 Lb_Battery 字段

### P0 运动控制适配器实现 (14:45)
- **CanMotionControllerAdapter** 从空壳变为完整实现：
  1. Connect(): 根据 CommType 调用 MT_Comm.InitCan(125,5) 或 InitCom(port)，自动懒初始化 MT_Comm 实例
  2. Disconnect(): 停止心跳 → MT_Comm.CloseSet()
  3. MoveForward(): SendData(1,1,6,0,"1") → CAN帧 43 52 01 ...
  4. MoveBackward(): SendData(1,1,6,0,"2") → CAN帧 43 52 02 ...
  5. Stop(): SendData(1,1,6,0,"3") → CAN帧 43 52 03 ...
  6. 心跳: System.Threading.Timer 每 1.5s 发 SendData(1,3,0,0,"99")，防止 MCU 超时断线
  7. SetSpeed: SendData(2,2,0,0,"speed,50") 写手动速度参数
  8. 顺带实现 SetGratingArm / SetGratingRange / SetGratingCorrection / MarkDefect / SetLightPosition
- 实现 IDisposable 确保资源释放
- **Frm_Main_C_New.cs**: Btn_Link 改为同时连接 TOFD + 运动控制；添加 OnFormClosing 断开所有硬件

### CAN_ChannelSend 调用链分析 (15:04)
- 完整链路: MT_Comm.SendData() → 拼帧头 → COM:RS232.WriteComm / CAN:Can_Send()
- Can_Send() 在 MT_Comm.cs:539-566 行，拆 hex 字符串为 8 字节 CAN_DataFrame
- CAN_ChannelSend() 是 [DllImport("CanCmd.dll")] P/Invoke 声明在 CanCmd.cs:145-146 行
- 位置: Clb_MT_Comm/Clb_MT_Comm/MT_Comm.cs:558

### 架构设计文档 PDF 生成 (15:40)
- 梳理新框架完整设计并输出为 PDF 文档 (6 页, 246KB)
- 包含: 分层架构、DI 组装、接口定义、CAN 协议、调用链路、状态管理、演进路线图
- 文件位置: doc/Tofd_AWI_新框架架构设计文档.pdf + .html
- 使用 fpdf2 生成，需注册 CJK 字体 (Microsoft YaHei, SimHei)

### 小车运动按钮调试 — 静默失败修复 (15:55)
- **问题**：点击 Forward/Backward/Stop 按钮，`CanMotionControllerAdapter.MoveForward()` 未被调用
- **根因**：`CanSend` 门控 (`_mtComm != null && IsConnected`)，未连接时静默 `return`；UI 层无任何反馈
- **修复**（3 个文件修改）：
  1. `MotionService.Forward/Backward/Stop` 改为返回 `bool`，未连接时返回 `false`（不更新方向状态）
  2. `Frm_Main_C_New` 运动按钮 handler 加 `_motionState.IsOnline` 前置检查，弹 MessageBox 提示"请先连接"
  3. `CanMotionControllerAdapter` MoveForward/Backward/Stop 加 `Debug.WriteLine` 追踪日志
- 同时同步更新了 `NewFramework/Services/MotionService.cs` 保持一致

### 下一步
- 在 VS 中重新生成解决方案，确认编译通过
- 逐步将 Frm_Main_C 按钮事件迁移到服务层
- 恢复 Tofd_DLL 引用后可填充适配器的 stub 方法

### PictureBox 布局从代码迁移到设计器 (16:57)
- **用户需求**: 三个显示组件的 PictureBox 应在 VS 设计器里布局，不要写在代码里
- **架构调整**:
  - Designer.cs: 更新 Pic_AScan(450x300)/Pic_BScan(450x300)/Pic_DScan(450x180)，新增 Pic_CScan(450x180)/Pic_InspectVideo(330x250)/Pic_OperatorVideo(330x250)
  - Frm_Main_C_New.cs: DI 构造函数不再接收 VideoDisplayService/WaveformDisplayService 参数，改为在 InitializeComponent() 后用 `this.Pic_XXX` 创建服务
  - Program_New.cs: 移除 VideoDisplayService 和 WaveformDisplayService 的外部创建和注入
  - VideoDisplayService.cs/WaveformDisplayService.cs: Dispose 不再释放 PictureBox（归窗体设计器管理）
- **架构原则**: UI 控件生命周期归设计器管理，服务层仅持有引用做渲染，不负责创建/销毁控件
- **编译结果**: ✅ 0 error (仅 MVSDK 项目的 .NET 2.0 SDK 预存环境错误)

### 编译错误修复 — SysInfo 迁移遗留问题 (16:27)
- **错误1**: `ClassInterFace` 在 `ClassLib_TestData` 和 `ClassLibrary_Interface` 之间不明确引用
  - 根因: `GridHelper.cs` 同时 `using ClassLib_TestData;` 和 `using ClassLibrary_Interface;`，两个命名空间都有 `ClassInterFace`
  - 修复: `GridHelper.cs:24` — 改为 `new ClassLibrary_Interface.ClassInterFace()` 完全限定
  
- **错误2**: 未能找到 `Cl_Print_Item` 和 `Cl_Print_Record`
  - 根因: InfrastructureModels.cs 改名为 `ClPrintItem`/`ClPrintRecord`，属性名也变了 (`strItemName`→`ItemName`, `blCheck`→`IsChecked`, `strRecordName`→`RecordName`)，但 GridHelper.cs 仍用旧名
  - 修复: GridHelper.cs 两处 GrdAddData 重载改用 `ClassLibrary_Interface.Models.ClPrintItem` 和 `ClassLibrary_Interface.Models.ClPrintRecord`，同步修正属性名

- **错误3**: `NewInspect.Services.dll` 元数据文件缺失
  - 根因: `ClassLib_DataMang.csproj` 目标框架 v4.5.2，环境中无对应 SDK
  - 修复: 升级 `ClassLib_DataMang.csproj` TargetFrameworkVersion 从 v4.5.2 → v4.8

- **错误4-7**: SysUtility.cs — struct null 比较 (CS0019) + switch 递归模式 (CS8370)
  - `SEmatChanParam` 是结构体，不能用 `== null` / `!= null` 比较 → 直接移除 null 检查
  - `switch expression` 需 C# 8.0+ → 改为传统 switch 语句

- **编译结果**: NewInspect.Services.dll ✅ 0 error, 2 warnings (预存)
